name: recurseo
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # State Management
  flutter_riverpod: ^2.6.1

  # Routing
  go_router: ^14.6.2

  # HTTP & API
  dio: ^5.7.0

  # Local Storage
  shared_preferences: ^2.3.3

  # Utils
  intl: ^0.19.0
  equatable: ^2.0.7

  # Firebase (opcional para MVP)
  # firebase_core: ^3.8.1
  # firebase_auth: ^5.3.3
  # cloud_firestore: ^5.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
